module.exports.execute = function ({ res, arg, config  }) {
    // const User = require('./api/conte/user.js').User
    // const Sequelize = require('sequelize');
    // const sequelize = new Sequelize('database', 'user', 'password', {
    //     host: 'localhost',
    //     dialect: 'sqlite',
    //     logging: false,
    //     // SQLite only
    //     storage: config.path.db,
    // });
    // User.sync()
    // User.create({
    //     login: 'test',
    //     password: 'test',
    //     email: 'test@test.com'
    // }).then(user => {
    //     console.log(user.toJSON());
    // })
}